import React from 'react';

const MakeAdmin = () => {
    return (
        <div>
            <input type="email" />
            <button className='submit'>Make Admin</button>
        </div>
    );
};

export default MakeAdmin;